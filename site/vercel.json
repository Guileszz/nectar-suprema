{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ],
  "functions": {
    "api/*.ts": {
      "maxDuration": 30
    }
  },
  "buildCommand": "npx tsc -b && vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite"
}